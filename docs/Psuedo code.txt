Part One:
FUNCTION GeneratePermutations(n):
	
	// check if n is greater than 1
	IF (n <= 1) :		
Return {};

	// initialize first array
	array = (0, 1, … n-1)
	Permutations = ArrayList<>
	
	Last = n-1
	
	// loop until array is in descending order
	WHILE (true):
		// save array in permutation
		permutations.add(arrays.copyOf(array, n))
		
		// set pointer
		Int j = last - 1

		// scan from right to left to find where left < right
		WHILE (j >= 0 && array[j] >= array[j+1]:
			j –
		
		// if j is less than 0 the array is in descending order and we would therefore have all permutations
		IF (j < 0):
			Break


		// set pointer 2
		Int k = last;
		// scan right to left for first number greater than j
		WHILE (array[j] >= array[k]:
			K–
		
		// swap j and k in the array
		Int temp = array[j]
		Array[j] = array[k]
		Array[k] = temp

		// Reverse everything after j + 1
		Int left = j + 1
		Int right = last

		WHILE (left < right):
			Temp = array[left]
			Array[left] = array[right]
			Array[right] = temp
			Left++
			right–
			             
Part Two:
FUNCTION mergeSort():
Referencing: https://www.geeksforgeeks.org/dsa/merge-sort/

FUNCTION quickSort():
Referencing: https://www.geeksforgeeks.org/dsa/quick-sort-algorithm/

FUNCTION shakerSort():
Referencing: https://www.geeksforgeeks.org/dsa/cocktail-sort/

FUNCTION heapSort():
Referencing: https://www.geeksforgeeks.org/dsa/heap-sort/
